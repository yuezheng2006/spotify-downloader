{
  "name": "spotdl-enhanced",
  "description": "spotDL Enhanced Web UI with complete metadata management",
  "plan_type": "nodejs",
  "build_command": "pip install -e . && pip install fastapi uvicorn",
  "start_command": "python3 web_enhanced.py --host 0.0.0.0 --port ${PORT}",
  "install_command": "apt-get update && apt-get install -y ffmpeg",
  "framework": "fastapi"
}

